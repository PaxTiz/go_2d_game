package utils

const WINDOW_WIDTH = 800
const WINDOW_HEIGHT = 640

const TEXTURE_SCALING = 2

func Dummy(vars ...any) {
	// Dummy function for prevent the Go compiler to screams when a variable is not used
}
